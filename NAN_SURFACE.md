# Ñan surface map — where this thread lives

Doctrine v11 LOCKED. Conjecture 1 OPEN. energy UNAVAILABLE. proven_trust false.

## Routing (locked)

| Surface | URL | Role |
|---|---|---|
| **SOURCE** | https://github.com/szl-holdings/szl-khipu | Canonical Python. One-file lab is `szl_khipu/nan_lab.py`. Kernels already on main @ `01ec801`. |
| **PRODUCT evaluate** | https://a-11-oy.com | Product runtime / console. Pointer for evaluate. Not a proof registry. |
| **PROOF RECORD** | https://a11oy.net/khipu/ | RECORD only. Chip: **KERNEL NOT RUN HERE**. CUDA UNAVAILABLE. |
| **HUB mirror** | https://huggingface.co/SZLHOLDINGS/szl-khipu | Package card + artifacts. Not a live Ñan Space from this session. |

Do **not** treat `github.com/SZLHoldings` as the org — that slug 404s. Org is `szl-holdings`.

HF write from this Grok session is **0**. Existing `publish-hf.yml` on the repo is the only honest Hub path. Do not claim a new Space is live with these 22 cuts until that workflow lands them.

## Cuts in `nan_lab.py` (22)

draft quorum latent breath echo ash rite gaze pacha tinku kuti yawar wasi sami kancha rimay nina suyay huklla ouroboros codex estate

Ouroboros fixture: 220 fail + 900 ok / wall 1300 → model 1120 · peak 900 · overhead 180 · serialization 220 · dead-hop 220.

## Run

```
python3 -m szl_khipu.nan_lab --selftest   # must print OK 22
python3 -m szl_khipu.nan_lab --serve 8765
```

Mint only while HOLDS. Never stamp a joule.
